from django.urls import path
from . import views

app_name = 'ai_service'

urlpatterns = [
    # Add AI service specific URLs here if needed
    # For now, the AI functionality is integrated into other apps
] 